// Package probe は、SPEC §20 の要検証事項を確かめるテスト（プローブ）を置く。
//
// プローブは fsops の実装を使わず、OS の API（x/sys、LazyDLL）を直接呼んで動作を調べる。
// 結果は t.Logf で "Vn:" で始まる行として出力する。CI では go test -v で実行してログを残す。
// 安全に関わる前提（リンク先が消えないことなど）は、ログだけでなくテストの失敗として検出する。
package probe
