# fsops を Claude Code で作る手順

あなた（開発者）用の手順書です。フェーズごとのプロンプトをコピーして Claude Code に渡してください。

---

## 0. 準備（Mac で 1 回だけ）

1. Go を入れる
   ```sh
   brew install go
   go version
   ```
2. Xcode コマンドラインツールを入れる（macOS のごみ箱処理で cgo を使うため）
   ```sh
   xcode-select --install
   ```
3. GitHub CLI を入れてログインする（Claude Code が CI の結果を読むため）
   ```sh
   brew install gh
   gh auth login
   ```
4. GitHub にリポジトリを作る。**公開か非公開か**を決めておく（非公開の場合、macOS の CI は手動・PR 時だけにする。SPEC §19）。
5. リポジトリを clone し、`CLAUDE.md`、`docs/SPEC-fsops.md`、`docs/PROMPTS.md`（このファイル）を配置してコミット・push する。
6. リポジトリ直下で Claude Code を起動する。
   ```sh
   claude
   ```

---

## 1. 進め方のコツ

- **フェーズの最初はプランモード**にする（Shift+Tab で切り替え）。計画を読んで納得してから実装に進ませる。
- **フェーズが終わったら `/clear`** して会話をリセットしてから次のフェーズへ。CLAUDE.md は毎回自動で読まれるので、前提は失われない。
- Claude Code が「要検証事項（V1〜V15）」の結果を報告してきたら、SPEC §20 に結果を追記させる。
- フェーズ6以降の完了報告には、不変条件の経路とテストの対応表が含まれる（CLAUDE.md）。テストで守られていない経路がないかを確認する。
- 迷ったら「§2 の不変条件を破らないか」を基準に判断する。

### フェーズの一覧

| # | 内容 | 要検証事項 |
|---|---|---|
| 0 | 計画の確認（実施済み） | |
| 1 | 土台 | |
| 2 | テスト用の道具、CI のボリューム、種類の判定 | V3・V7・V8 |
| 3 | 要検証事項の確認（プローブのみ） | V1・V2・V4・V5・V6・V10〜V15 |
| 4 | パス、fileID、排他リネーム、Rename | |
| 5 | 走査と計画（NewPlan） | |
| 6 | Execute の骨格、進捗とキャンセル、完全削除 | |
| 7 | コピー①：ファイル・フォルダ・衝突 | |
| 8 | コピー②：リンク・メタデータ・検証・容量 | |
| 9 | 移動 | |
| 10 | ごみ箱 | |
| 11 | 動作確認用 CLI と総点検 | |

---

## 2. フェーズごとのプロンプト

### フェーズ0: 計画の確認（コードは書かない）※実施済み

```
CLAUDE.md と docs/SPEC-fsops.md を全部読んでください。まだコードは書かないでください。
次の3点を報告してください。
1. SPEC の API 案（§5）とファイル構成案（§4）について、Go の慣習や実装の都合から改善したほうがよい点
2. SPEC の中で矛盾している箇所、または曖昧で実装時に判断が分かれる箇所
3. このあとのフェーズの順序や区切りで、変えたほうがよい点
SPEC を変更する提案がある場合は、変更後の文面まで示してください。私が承認してから SPEC を更新してください。
```

承認した変更を SPEC に反映させたら、コミットさせて `/clear`。

### フェーズ1: 土台

`<...>` の部分を書き換えてから渡してください。

```
フェーズ1を始めます。docs/SPEC-fsops.md の §4、§5、§17、§19 を読んでください。
モジュールパスは <github.com/あなたのID/リポジトリ名> です。リポジトリは <公開 / 非公開> です。
既定のブランチ名は <main / master> にします。

やること:
- go.mod の作成（Go はインストール済みの最新安定版）
- .gitignore の作成（.DS_Store など）
- internal/fsops の型定義（§5）。Plan のフィールドは非公開、決定は Decide で設定する。
  処理本体はまだ書かず、関数は未実装を示すエラーを返すだけでよい
- testHooks の型（hooks.go）。中身は空でよい
- エラー分類（§17）。Windows と Unix のエラー番号の対応、KindOf、各型の String() を含む
- エラー分類のテスト
- deps_test.go（§4 の依存の許可リスト。GOOS と CGO_ENABLED の組み合わせごと）
- .github/workflows/test.yml（§19。この段階では windows・macos の go vet と go test、ubuntu ジョブの gofmt とコンパイル確認だけ。
  ubuntu ジョブのごみ箱のテストはフェーズ10で追加する）

完了条件:
- CLAUDE.md の「コマンド」の確認がすべて手元で通る
- push して GitHub Actions が成功する（非公開の場合、macOS は workflow_dispatch で手動実行して確認）
```

### フェーズ2: テスト用の道具と CI 環境

```
フェーズ2です。docs/SPEC-fsops.md の §14.1、§18、§19、§20 を読んでください。

やること:
- internal/fsops/internal/testfs にテスト用フィクスチャを作る（§18.1）
- エントリの種類の判定（§14.1）を実装する
- CI に、ボリュームをまたぐテスト用の領域の作成を追加する（Windows: VHD、macOS: hdiutil）。FSOPS_CROSSVOL_DIR と FSOPS_TEST_TRASH を設定する
- フィクスチャと種類の判定のテスト
- 要検証事項 V3、V7、V8 を確かめるテストを書く

完了条件:
- CI が両 OS で成功する
- V3、V7、V8 の結果を報告する。結果に応じて SPEC を変えるべき点があれば、変更案を示す
```

### フェーズ3: 要検証事項の確認（プローブのみ）

```
フェーズ3です。docs/SPEC-fsops.md の §2、§8、§12、§15、§20 を読んでください。

やること:
- 要検証事項 V1、V2、V4、V5、V6、V10、V11、V12、V13、V14、V15 を確かめるテスト（プローブ）を書き、CI で実行する
- fsops の本体のコードは書かない。プローブは internal/fsops/internal/probe の _test.go に置き、
  x/sys や LazyDLL を直接呼んでよい
- V12・V14（exFAT・FAT32 のボリューム）と V13（ごみ箱の容量・無効化の設定）は、テストが触ってよい範囲の外
  （新しいボリュームのマウント、CI ランナーのレジストリ）を使う必要がある。
  実装の前に、用意する方法（環境変数、CI の手順）を示して私の承認を取る
- ごみ箱を使うプローブは FSOPS_TEST_TRASH=1 のときだけ実行する

完了条件:
- 各 V の結果を表にして報告する（確認できたこと、できなかったこと、CI の実行へのリンク）
- 結果に応じて SPEC を変えるべき点（§8.4、§12.2、§15 など）の変更案を示す。私が承認したら SPEC と §20 を更新する
- プローブを回帰テストとして残すか消すかを提案する
```

### フェーズ4: パス、fileID、排他リネーム

```
フェーズ4です。docs/SPEC-fsops.md の §2、§8、§11.3、§17、§20（V1、V2、V11、V12 の結果）を読んでください。

やること:
- 絶対パスの検査（§8.1。デバイスパスとボリュームのルートの拒否を含む）、\\?\ 形式への変換 helper（§8.2）
- fileID（§8.3。Windows は FileIdInfo）と、祖先の判定（Windows は GetFinalPathNameByHandle）。os.SameFile は使わない
- 排他リネームと置換リネーム（§8.4）
- 新しい名前の検査と Rename 関数（§11.3）
- §18.4 の表のうち「パス」「I6」の行と、「コピー先がジャンクション経由でコピー元の内側」の判定部分のテスト。
  末尾が . や空白の名前・予約名の行は、この段階では helper を通した Lstat・Remove・Rename が同名の別ファイルに影響しないことを確かめる

テストを先に書き、失敗することを確認してから実装してください。

完了条件:
- CI が両 OS で成功する
```

### フェーズ5: 走査と計画（NewPlan）

```
フェーズ5です。docs/SPEC-fsops.md の §2、§6、§8.3、§9、§12.1、§13.1、§14.1、§20（V14 の結果）を読んでください。

やること:
- リンクに入り込まない走査（§13.1）の列挙部分。削除のためにハンドルで確かめて入る部分はフェーズ6で作る
- NewPlan（§6）。Item.Err、ConflictID 付きの衝突の検出、上書き先の fileID の記録、DestInsideSource、空き容量の見込み（§6.4）を含む
- ごみ箱が使えるかの計画時の事前確認（§12 のうち、ファイルシステムを変更しない確認だけ）
- Decide と、決定の妥当性チェック（§9.1 の表）
- §18.4 の表のうち「計画」「衝突」の行のうち、計画だけで確かめられるもののテスト
- 「計画の作成前後でファイルシステムが変化しない」テスト

完了条件:
- CI が両 OS で成功する
```

### フェーズ6: Execute の骨格と完全削除

```
フェーズ6です。docs/SPEC-fsops.md の §2、§7、§13、§14.2、§16、§20（V15 の結果）を読んでください。

やること:
- Execute の骨格（§7.1 の実行前の検査、§7.2 の処理順、§7.4 の結果と Status）
- 進捗とキャンセル（§16）
- 削除のための走査（§13.1 の、開いたハンドルで確かめてから入る方式と、Windows のハンドルによる列挙。フェーズ9の同一ボリュームのマージ移動でも使うので、削除と移動の両方から使える形にする）
- OpDelete（§13.2）と、記録した項目だけの削除（§13.3。記録との照合を含む。フェーズ9の移動で使う）
- §18.4 の表のうち、完全削除に関する I4 の行（ジャンクション・シンボリックリンクの先の目印ファイルが残ること、
  フォルダをリンクに置き換える注入）のテストを最初に書く
- 読み取り専用ファイル、使用中のファイルを含むツリーの削除のテスト、削除中のキャンセルのテスト

完了条件:
- CI が両 OS で成功する
- ジャンクションを含むツリーの削除で、ジャンクション先の中身が残ることが Windows の CI で確認できている
- 不変条件の経路とテストの対応表を報告する
```

### フェーズ7: コピー①（ファイル・フォルダ・衝突）

```
フェーズ7です。docs/SPEC-fsops.md の §2、§7、§9、§10.1、§10.2、§10.5、§16 を読んでください。

やること:
- ファイルのコピー（§10.1。一時ファイル、排他リネーム）とフォルダのコピー（§10.2）
- 衝突の決定（Skip / Overwrite / AutoRename / Merge）の処理（§9）。上書き・マージの直前の fileID の照合（§7.3）を含む
- 同期（§10.5 のコピーの場合）
- §18.4 の表のうち I1、I3、衝突、ロック、読み取り専用の上書き先の行のテスト

不変条件 I1 と I3 のテストを最初に書いてください。

完了条件:
- CI が両 OS で成功する（macOS は -race 付き）
- 不変条件の経路とテストの対応表を報告する
```

### フェーズ8: コピー②（リンク・メタデータ・検証・容量）

```
フェーズ8です。docs/SPEC-fsops.md の §2、§7.2、§10.3、§10.4、§14、§15、§20（V6 の結果）を読んでください。

やること:
- リンクと特殊なファイルの扱い（§14.2 のコピーの列。Windows のリンクのファイル用・フォルダ用の区別を含む）
- メタデータ（§15）。Zone.Identifier と com.apple.quarantine は、フェーズ3の V6 の結果に基づいて決めた方針に従う
- 検証（§10.4）
- 容量不足（§10.3、§7.2）
- §18.4 の表のうち、コピーに関する I4、メタデータ、リンク、検証、容量、読み取り専用ファイルのコピーの行のテスト

完了条件:
- CI が両 OS で成功する（macOS は -race 付き）
- 不変条件の経路とテストの対応表を報告する
```

### フェーズ9: 移動

```
フェーズ9です。docs/SPEC-fsops.md の §2、§10.5、§11.1、§11.2、§13.3、§14.2 を読んでください。

やること:
- 同一ボリュームの移動（§11.1）。ボリューム違いのエラー時の切り替えと、マージ移動で §13.1 の方式で入り込むことを含む
- ボリュームをまたぐ移動（§11.2）。同期（§10.5）と、記録した項目だけの削除（§13.3）を使う
- §18.4 の表のうち I2 のすべての行と、移動に関する I4 の行（マージ移動でのリンクへの置き換えの注入を含む）のテスト。これらを最初に書く

完了条件:
- FSOPS_CROSSVOL_DIR を設定した CI が両 OS で成功する
- 手元の Mac でも §18.3 の手順でボリュームをまたぐテストが通る
- 不変条件の経路とテストの対応表を報告する
```

### フェーズ10: ごみ箱

```
フェーズ10です。docs/SPEC-fsops.md の §2、§12、§19、§20（V4、V5、V10、V13 の結果）を読んでください。

やること:
- Windows（§12.2）、macOS（§12.3。cgo あり・なしの両方）、その他の OS（§12.4）のごみ箱処理。
  フェーズ3の V4・V13 の結果に基づいて決めた方式に従う。Win32 の正規化で変わる名前を含むパスの事前確認（§12.2）を含む
- §18.4 の表のうち I5 と「ごみ箱」の行、ごみ箱に関する I4 の行のテスト
- CI に、ごみ箱が使えないことを確かめるテストの実行を追加する（ubuntu ジョブ、macOS の CGO_ENABLED=0。§19）

完了条件:
- CI が両 OS と ubuntu で成功する
- 固定ドライブ以外のパス、ごみ箱の容量を超える項目、ごみ箱が無効なボリュームで、黙って完全削除されないことが確認できている
- 不変条件の経路とテストの対応表を報告する
```

### フェーズ11: 動作確認用 CLI と総点検

```
フェーズ11です。

やること:
1. cmd/fsopsctl を作る。サブコマンドは plan / copy / move / trash / delete。
   - plan は計画と衝突の一覧を表示するだけで、何も変更しない
   - 衝突の扱いは --on-conflict=skip|overwrite|rename|merge で一括指定する（Decide で設定する）。
     その衝突の種類で許されない決定（§9.1）になる衝突は Skip のままにし、その旨を表示する
   - trash で Item.Err が KindTrashUnavailable の項目は、完全削除に切り替えずに表示する
   - delete は --yes がなければ実行しない
   - 進捗は標準エラー出力、結果の一覧は標準出力。表示は日本語
2. 不変条件の総点検。§2 の I1〜I7 それぞれについて、それを破りうるコード経路と、それを防いでいるテストの対応表を docs/fsops-invariants.md に作る。
   各フェーズの報告の対応表をまとめ直し、テストで守られていない経路があれば列挙する（修正はまだしない）
3. UI 側から fsops を使うための説明を internal/fsops/doc.go に書く

完了条件:
- CI が両 OS で成功する
- docs/fsops-invariants.md の内容を報告する
```

総点検で見つかった穴は、個別に直させてください。

---

## 3. 困ったとき用のプロンプト

### CI の Windows だけ失敗する

```
GitHub Actions の Windows ジョブが失敗しました。gh run view --log-failed でログを確認し、原因を特定して報告してください。
修正の前に、なぜ macOS では通ったのかを説明してください。
Windows 固有の挙動が原因なら、それを再現する最小のテストを先に追加してから直してください。
```

### テストがときどき失敗する

```
<テスト名> が CI でときどき失敗します。gh で直近の失敗ログを集め、原因の候補を挙げてください
（タイミング依存、ウイルス対策ソフトによる一時的なロック、一時フォルダの扱いなど）。
テストにスリープやリトライを足して隠す修正はしないでください。
fsops の実装側で共有違反に短いリトライを入れるべきだと考える場合は、その案と不変条件への影響を示して私に確認してください。
```

### 仕様を変えたい

```
<変えたい内容> のように仕様を変えたいです。
SPEC のどの節に影響するか、§2 の不変条件と衝突しないかを確認し、SPEC の変更案とテストへの影響を示してください。
私が承認するまで実装しないでください。
```

### 不変条件の監査（いつでも）

```
現在の internal/fsops のコードを、docs/SPEC-fsops.md §2 の不変条件 I1〜I7 の観点だけで見直してください。
各不変条件について、それを破りうるコード経路と、それを防いでいるテストを対応づけて示し、
テストで守られていない経路があれば列挙してください。修正はまだしないでください。
```

---

## 4. CI 以外での確認（リリース前）

CI では確かめられないものがあります（SPEC §20 の V9 など）。

- 日本語ロケールの Windows での動作
- OneDrive の同期フォルダ（未ダウンロードのファイル）
- ネットワークドライブ、USB メモリ（Windows の FAT32・exFAT、macOS の exFAT）

UTM などの仮想マシンの Windows、または実際に使う Windows PC で、フェーズ11の `fsopsctl` を使って確認してください。
Windows 用のバイナリは Mac 上で作れます。

```sh
GOOS=windows GOARCH=amd64 go build -o fsopsctl.exe ./cmd/fsopsctl   # 一般的な PC 用
GOOS=windows GOARCH=arm64 go build -o fsopsctl.exe ./cmd/fsopsctl   # Apple Silicon の Mac 上の Windows 用
```

macOS 用のバイナリは、ごみ箱に cgo を使うため、Mac 上で `CGO_ENABLED=1`（既定）でビルドしてください。
ほかの OS からクロスコンパイルすると、ごみ箱が常に使えないビルドになります。
