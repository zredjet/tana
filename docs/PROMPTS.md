# tana を Claude Code で作る手順

あなた（開発者）用の手順書です。フェーズごとのプロンプトをコピーして Claude Code に渡してください。
フェーズ0〜11 で fsops（ファイル操作）を作り、フェーズ12 からは TUI の土台とファイラー本体を作ります。

---

## 0. 準備（Mac で 1 回だけ）

1. Go を入れる
   ```sh
   brew install go
   go version
   ```
2. Xcode コマンドラインツールを入れる（macOS のごみ箱処理で cgo を使うため）
   ```sh
   xcode-select --install
   ```
3. GitHub CLI を入れてログインする（Claude Code が CI の結果を読むため）
   ```sh
   brew install gh
   gh auth login
   ```
4. GitHub にリポジトリを作る。**公開か非公開か**を決めておく（非公開の場合、macOS の CI は手動・PR 時だけにする。SPEC §19）。
5. リポジトリを clone し、`CLAUDE.md`、`docs/SPEC-fsops.md`、`docs/PROMPTS.md`（このファイル）を配置してコミット・push する。
6. リポジトリ直下で Claude Code を起動する。
   ```sh
   claude
   ```

---

## 1. 進め方のコツ

- **フェーズの最初はプランモード**にする（Shift+Tab で切り替え）。計画を読んで納得してから実装に進ませる。
- **フェーズが終わったら `/clear`** して会話をリセットしてから次のフェーズへ。CLAUDE.md は毎回自動で読まれるので、前提は失われない。
- Claude Code が要検証事項の結果を報告してきたら、それぞれの仕様書に結果を追記させる。
  fsops の V は SPEC-fsops §20、ファイラーの VU は SPEC-filer §12、TUI の土台の VT は SPEC-tui §9。
- フェーズ6以降の完了報告には、不変条件の経路とテストの対応表が含まれる（CLAUDE.md）。テストで守られていない経路がないかを確認する。
  TUI の土台のフェーズ（13〜16）では T1〜T6、ファイラー本体のフェーズ（19・20）では U1〜U6 の表も含まれる。
- 迷ったら「SPEC-fsops §2 の不変条件を破らないか」を基準に判断する。

### フェーズの一覧

| # | 内容 | 要検証事項 |
|---|---|---|
| 0 | 計画の確認（実施済み） | |
| 1 | 土台 | |
| 2 | テスト用の道具、CI のボリューム、種類の判定 | V3・V7・V8 |
| 3 | 要検証事項の確認（プローブのみ） | V1・V2・V4・V5・V6・V10〜V15 |
| 4 | パス、fileID、排他リネーム、Rename | |
| 5 | 走査と計画（NewPlan） | |
| 6 | Execute の骨格、進捗とキャンセル、完全削除 | |
| 7 | コピー①：ファイル・フォルダ・衝突 | |
| 8 | コピー②：リンク・メタデータ・検証・容量 | |
| 9 | 移動 | |
| 10 | ごみ箱 | |
| 11 | 動作確認用 CLI と総点検 | |
| 12 | 端末の実測（term の最小部分と tuiprobe） | VU2・VU3・VU6、VT1・VT2・VT6 |
| 13 | textwidth（書記素クラスタと表示幅） | |
| 14 | keys（入力の解釈） | |
| 15 | screen と lineedit | |
| 16 | term の完成と tui の土台、確認用の画面 | VU1・VU4・VU5・VU9、VT3〜VT5・VT7 |
| 17 | fsops への追加（F1・F2） | |
| 18 | 読むだけのファイラー。画面の構成とキーの系統を決める | VU7・VU8・VU10 |
| 19 | コピー・移動 | |
| 20 | ごみ箱・完全削除・名前の変更・新しいフォルダ | |
| 21 | 手動の確認と仕様への反映 | |

フェーズ12以降の内容は SPEC-filer §13 と SPEC-tui §10 が正です。
フェーズ12以降は、仮想マシンでの確認（§5）も使います。

---

## 2. フェーズごとのプロンプト

### フェーズ0: 計画の確認（コードは書かない）※実施済み

```
CLAUDE.md と docs/SPEC-fsops.md を全部読んでください。まだコードは書かないでください。
次の3点を報告してください。
1. SPEC の API 案（§5）とファイル構成案（§4）について、Go の慣習や実装の都合から改善したほうがよい点
2. SPEC の中で矛盾している箇所、または曖昧で実装時に判断が分かれる箇所
3. このあとのフェーズの順序や区切りで、変えたほうがよい点
SPEC を変更する提案がある場合は、変更後の文面まで示してください。私が承認してから SPEC を更新してください。
```

承認した変更を SPEC に反映させたら、コミットさせて `/clear`。

### フェーズ1: 土台

`<...>` の部分を書き換えてから渡してください。

```
フェーズ1を始めます。docs/SPEC-fsops.md の §4、§5、§17、§19 を読んでください。
モジュールパスは <github.com/あなたのID/リポジトリ名> です。リポジトリは <公開 / 非公開> です。
既定のブランチ名は <main / master> にします。

やること:
- go.mod の作成（Go はインストール済みの最新安定版）
- .gitignore の作成（.DS_Store など）
- internal/fsops の型定義（§5）。Plan のフィールドは非公開、決定は Decide で設定する。
  処理本体はまだ書かず、関数は未実装を示すエラーを返すだけでよい
- testHooks の型（hooks.go）。中身は空でよい
- エラー分類（§17）。Windows と Unix のエラー番号の対応、KindOf、各型の String() を含む
- エラー分類のテスト
- deps_test.go（§4 の依存の許可リスト。GOOS と CGO_ENABLED の組み合わせごと）
- .github/workflows/test.yml（§19。この段階では windows・macos の go vet と go test、ubuntu ジョブの gofmt とコンパイル確認だけ。
  ubuntu ジョブのごみ箱のテストはフェーズ10で追加する）

完了条件:
- CLAUDE.md の「コマンド」の確認がすべて手元で通る
- push して GitHub Actions が成功する（非公開の場合、macOS は workflow_dispatch で手動実行して確認）
```

### フェーズ2: テスト用の道具と CI 環境

```
フェーズ2です。docs/SPEC-fsops.md の §14.1、§18、§19、§20 を読んでください。

やること:
- internal/fsops/internal/testfs にテスト用フィクスチャを作る（§18.1）
- エントリの種類の判定（§14.1）を実装する
- CI に、ボリュームをまたぐテスト用の領域の作成を追加する（Windows: VHD、macOS: hdiutil）。FSOPS_CROSSVOL_DIR と FSOPS_TEST_TRASH を設定する
- フィクスチャと種類の判定のテスト
- 要検証事項 V3、V7、V8 を確かめるテストを書く

完了条件:
- CI が両 OS で成功する
- V3、V7、V8 の結果を報告する。結果に応じて SPEC を変えるべき点があれば、変更案を示す
```

### フェーズ3: 要検証事項の確認（プローブのみ）

```
フェーズ3です。docs/SPEC-fsops.md の §2、§8、§12、§15、§20 を読んでください。

やること:
- 要検証事項 V1、V2、V4、V5、V6、V10、V11、V12、V13、V14、V15 を確かめるテスト（プローブ）を書き、CI で実行する
- fsops の本体のコードは書かない。プローブは internal/fsops/internal/probe の _test.go に置き、
  x/sys や LazyDLL を直接呼んでよい
- V12・V14（exFAT・FAT32 のボリューム）と V13（ごみ箱の容量・無効化の設定）は、テストが触ってよい範囲の外
  （新しいボリュームのマウント、CI ランナーのレジストリ）を使う必要がある。
  実装の前に、用意する方法（環境変数、CI の手順）を示して私の承認を取る
- ごみ箱を使うプローブは FSOPS_TEST_TRASH=1 のときだけ実行する

完了条件:
- 各 V の結果を表にして報告する（確認できたこと、できなかったこと、CI の実行へのリンク）
- 結果に応じて SPEC を変えるべき点（§8.4、§12.2、§15 など）の変更案を示す。私が承認したら SPEC と §20 を更新する
- プローブを回帰テストとして残すか消すかを提案する
```

### フェーズ4: パス、fileID、排他リネーム

```
フェーズ4です。docs/SPEC-fsops.md の §2、§8、§11.3、§17、§20（V1、V2、V11、V12 の結果）を読んでください。

やること:
- 絶対パスの検査（§8.1。デバイスパスとボリュームのルートの拒否を含む）、\\?\ 形式への変換 helper（§8.2）
- fileID（§8.3。Windows は FileIdInfo）と、祖先の判定（Windows は GetFinalPathNameByHandle）。os.SameFile は使わない
- 排他リネームと置換リネーム（§8.4）
- 新しい名前の検査と Rename 関数（§11.3）
- §18.4 の表のうち「パス」「I6」の行と、「コピー先がジャンクション経由でコピー元の内側」の判定部分のテスト。
  末尾が . や空白の名前・予約名の行は、この段階では helper を通した Lstat・Remove・Rename が同名の別ファイルに影響しないことを確かめる

テストを先に書き、失敗することを確認してから実装してください。

完了条件:
- CI が両 OS で成功する
```

### フェーズ5: 走査と計画（NewPlan）

```
フェーズ5です。docs/SPEC-fsops.md の §2、§6、§8.3、§9、§12.1、§13.1、§14.1、§20（V14 の結果）を読んでください。

やること:
- リンクに入り込まない走査（§13.1）の列挙部分。削除のためにハンドルで確かめて入る部分はフェーズ6で作る
- NewPlan（§6）。Item.Err、ConflictID 付きの衝突の検出、上書き先の fileID の記録、DestInsideSource、空き容量の見込み（§6.4）を含む
- ごみ箱が使えるかの計画時の事前確認（§12 のうち、ファイルシステムを変更しない確認だけ）
- Decide と、決定の妥当性チェック（§9.1 の表）
- §18.4 の表のうち「計画」「衝突」の行のうち、計画だけで確かめられるもののテスト
- 「計画の作成前後でファイルシステムが変化しない」テスト

完了条件:
- CI が両 OS で成功する
```

### フェーズ6: Execute の骨格と完全削除

```
フェーズ6です。docs/SPEC-fsops.md の §2、§7、§13、§14.2、§16、§20（V15 の結果）を読んでください。

やること:
- Execute の骨格（§7.1 の実行前の検査、§7.2 の処理順、§7.4 の結果と Status）
- 進捗とキャンセル（§16）
- 削除のための走査（§13.1 の、開いたハンドルで確かめてから入る方式と、Windows のハンドルによる列挙。フェーズ9の同一ボリュームのマージ移動でも使うので、削除と移動の両方から使える形にする）
- OpDelete（§13.2）と、記録した項目だけの削除（§13.3。記録との照合を含む。フェーズ9の移動で使う）
- §18.4 の表のうち、完全削除に関する I4 の行（ジャンクション・シンボリックリンクの先の目印ファイルが残ること、
  フォルダをリンクに置き換える注入）のテストを最初に書く
- 読み取り専用ファイル、使用中のファイルを含むツリーの削除のテスト、削除中のキャンセルのテスト

完了条件:
- CI が両 OS で成功する
- ジャンクションを含むツリーの削除で、ジャンクション先の中身が残ることが Windows の CI で確認できている
- 不変条件の経路とテストの対応表を報告する
```

### フェーズ7: コピー①（ファイル・フォルダ・衝突）

```
フェーズ7です。docs/SPEC-fsops.md の §2、§7、§9、§10.1、§10.2、§10.5、§16 を読んでください。

やること:
- ファイルのコピー（§10.1。一時ファイル、排他リネーム）とフォルダのコピー（§10.2）
- 衝突の決定（Skip / Overwrite / AutoRename / Merge）の処理（§9）。上書き・マージの直前の fileID の照合（§7.3）を含む
- 同期（§10.5 のコピーの場合）
- §18.4 の表のうち I1、I3、衝突、ロック、読み取り専用の上書き先の行のテスト

不変条件 I1 と I3 のテストを最初に書いてください。

完了条件:
- CI が両 OS で成功する（macOS は -race 付き）
- 不変条件の経路とテストの対応表を報告する
```

### フェーズ8: コピー②（リンク・メタデータ・検証・容量）

```
フェーズ8です。docs/SPEC-fsops.md の §2、§7.2、§10.3、§10.4、§14、§15、§20（V6 の結果）を読んでください。

やること:
- リンクと特殊なファイルの扱い（§14.2 のコピーの列。Windows のリンクのファイル用・フォルダ用の区別を含む）
- メタデータ（§15）。Zone.Identifier と com.apple.quarantine は、フェーズ3の V6 の結果に基づいて決めた方針に従う
- 検証（§10.4）
- 容量不足（§10.3、§7.2）
- §18.4 の表のうち、コピーに関する I4、メタデータ、リンク、検証、容量、読み取り専用ファイルのコピーの行のテスト

完了条件:
- CI が両 OS で成功する（macOS は -race 付き）
- 不変条件の経路とテストの対応表を報告する
```

### フェーズ9: 移動

```
フェーズ9です。docs/SPEC-fsops.md の §2、§10.5、§11.1、§11.2、§13.3、§14.2 を読んでください。

やること:
- 同一ボリュームの移動（§11.1）。ボリューム違いのエラー時の切り替えと、マージ移動で §13.1 の方式で入り込むことを含む
- ボリュームをまたぐ移動（§11.2）。同期（§10.5）と、記録した項目だけの削除（§13.3）を使う
- §18.4 の表のうち I2 のすべての行と、移動に関する I4 の行（マージ移動でのリンクへの置き換えの注入を含む）のテスト。これらを最初に書く

完了条件:
- FSOPS_CROSSVOL_DIR を設定した CI が両 OS で成功する
- 手元の Mac でも §18.3 の手順でボリュームをまたぐテストが通る
- 不変条件の経路とテストの対応表を報告する
```

### フェーズ10: ごみ箱

```
フェーズ10です。docs/SPEC-fsops.md の §2、§12、§19、§20（V4、V5、V10、V13 の結果）を読んでください。

やること:
- Windows（§12.2）、macOS（§12.3。cgo あり・なしの両方）、その他の OS（§12.4）のごみ箱処理。
  フェーズ3の V4・V13 の結果に基づいて決めた方式に従う。Win32 の正規化で変わる名前を含むパスの事前確認（§12.2）を含む
- §18.4 の表のうち I5 と「ごみ箱」の行、ごみ箱に関する I4 の行のテスト
- CI に、ごみ箱が使えないことを確かめるテストの実行を追加する（ubuntu ジョブ、macOS の CGO_ENABLED=0。§19）

完了条件:
- CI が両 OS と ubuntu で成功する
- 固定ドライブ以外のパス、ごみ箱の容量を超える項目、ごみ箱が無効なボリュームで、黙って完全削除されないことが確認できている
- 不変条件の経路とテストの対応表を報告する
```

### フェーズ11: 動作確認用 CLI と総点検

```
フェーズ11です。

やること:
1. cmd/fsopsctl を作る。サブコマンドは plan / copy / move / trash / delete。
   - plan は計画と衝突の一覧を表示するだけで、何も変更しない
   - 衝突の扱いは --on-conflict=skip|overwrite|rename|merge で一括指定する（Decide で設定する）。
     その衝突の種類で許されない決定（§9.1）になる衝突は Skip のままにし、その旨を表示する
   - trash で Item.Err が KindTrashUnavailable の項目は、完全削除に切り替えずに表示する
   - delete は --yes がなければ実行しない
   - 進捗は標準エラー出力、結果の一覧は標準出力。表示は日本語
2. 不変条件の総点検。§2 の I1〜I7 それぞれについて、それを破りうるコード経路と、それを防いでいるテストの対応表を docs/fsops-invariants.md に作る。
   各フェーズの報告の対応表をまとめ直し、テストで守られていない経路があれば列挙する（修正はまだしない）
3. UI 側から fsops を使うための説明を internal/fsops/doc.go に書く

完了条件:
- CI が両 OS で成功する
- docs/fsops-invariants.md の内容を報告する
```

総点検で見つかった穴は、個別に直させてください。

### フェーズ12〜21 の前に

- フェーズ12とフェーズ13のプロンプトは、そのまま使えます。
- フェーズ14以降のプロンプトは、前のフェーズの結果（実測の結果や仕様の変更）を受けて、始める前に見直してください。
- 手で確かめる作業のうち、あなたが行うものは §5 にまとめています。

### フェーズ12: 端末の実測

```
フェーズ12です。docs/SPEC-filer.md の §2、§4、§9、§12、§13 と、docs/SPEC-tui.md の §1〜§5、§8〜§10 を読んでください。
CLAUDE.md の「仮想マシンでの確認」と、docs/PROMPTS.md の §5 も読んでください。

やること:
- リポジトリ全体の依存の検査（filer §4、tui §3）。fsops の deps_test.go と同じ方式で、許可リストは標準ライブラリ・x/sys・x/text。
  パッケージの間の依存の向きの検査は、そのパッケージを作るフェーズで足していく
- internal/term の最小部分（tui §8）: raw モードとその復元、Windows のコンソールモード、入力の生の読み取り
  （Unix はバイト列、Windows は ReadConsoleInputW のレコード）、大きさの取得、出力。
  Windows の出力は、VT1 を確かめられるよう、候補の方法を切り替えられるようにする
- cmd/tuiprobe の width と keys（filer §12.1）。結果は docs/probe-results/<端末>-<日付>.json に書く。
  keys は、Windows で VT の入力モードの有無を切り替えて記録できるようにする（VT2）。貼り付けも記録する（VT6）
- VU6 を確かめる CI のプローブ（対になっていないサロゲートを含む名前を作り、os.ReadDir で得た名前を fsops に渡して、同じエントリを指せるか）。
  置き場所は internal/fsops/internal/probe
- 測定:
  - Mac の Terminal.app と iTerm2 の width は、あなたが open -a で起動して行う
  - 仮想マシンの Windows Terminal と conhost の width は、あなたが行う（prlctl exec で起動し、computer use で撮影する）
  - 仮想マシンの keys は私が行うので、起動用のファイルを用意して、手順を示して私に頼む
  - Mac の端末の keys（IME で確定した日本語と貼り付けを含む）は私が行うので、手順を示して私に頼む

完了条件:
- CLAUDE.md の「コマンド」の確認がすべて通り、CI が 3 つの OS で成功する
- 4 つの端末（Terminal.app、iTerm2、Windows Terminal、conhost）の width と keys の結果が docs/probe-results にそろっている
- 結果の表を filer §12 と tui §9 に追記する案を示す
- VU2・VU3・VU6・VT1・VT2・VT6 の結果に応じて、仕様を変えるべき点の変更案を示す
  （textwidth の幅の規則、keys の Esc の待ち時間、罫線を使うかどうか、Windows の入出力の方法など）
```

### フェーズ13: textwidth

```
フェーズ13です。docs/SPEC-tui.md の §2〜§4、§10 と、docs/SPEC-filer.md の §9 を読んでください。
フェーズ12の結果（docs/probe-results と、filer §12・tui §9 への追記）も読んでください。

やること:
- Unicode のデータ（いまの最新の版）をリポジトリに置き、go generate で表を作る仕組み。
  データのダウンロードは、URL と大きさを示して私の許可を取ってから行う
- internal/textwidth: 書記素クラスタの分割、幅、分類（端末に出してはいけない文字、不正なバイト）、幅が端末によって違いうる印
- テストを先に書く: GraphemeBreakTest.txt のすべての行、フェーズ12の実測を期待値にした表、ファジング
- deps_test に textwidth の依存の向き（標準ライブラリだけ）を足す

完了条件:
- tui §10 の完了条件を満たす
- 実測と合わない書記素クラスタがあれば、どの端末の値を採ったかと、その理由を報告する
```

### フェーズ14: keys

```
フェーズ14です。docs/SPEC-tui.md の §2、§3、§5、§10 と、フェーズ12の keys の記録（docs/probe-results）を読んでください。

やること:
- internal/keys: バイト列と、Windows の入力のレコード（OS に依存しない型）を、イベントに変える状態機械
- テストを先に書く: 端末ごとの記録を入力にした表のテスト、T3 と「区切りに依存しない」性質のファジング
- Esc の待ち時間は、時刻を引数で渡してテストする（スリープしない）

完了条件:
- tui §10 の完了条件を満たす
- 記録の中に解釈できなかった入力があれば、一覧にして報告する
```

### フェーズ15: screen と lineedit

```
フェーズ15です。docs/SPEC-tui.md の §2、§3、§6、§7、§10 と、docs/SPEC-filer.md の §8.7、§9 を読んでください。

やること:
- internal/screen: セルの格子、領域、差分の出力。テスト用の小さな端末エミュレータ
- internal/lineedit
- テストを先に書く: T2・T5・T6 のファジング（幅の計算が合わない端末の模擬を含む）、lineedit の性質のファジング

完了条件:
- 両方のパッケージが tui §10 の完了条件を満たす
```

### フェーズ16: term の完成と tui の土台

```
フェーズ16です。docs/SPEC-tui.md の §2、§8〜§10 と、docs/SPEC-filer.md の §2、§4、§10、§12 を読んでください。

やること:
- internal/term の完成（終了時の読み取りの停止、大きさの変更、シグナル、T1）
- 疑似端末・疑似コンソールでの term のテスト（VT3。使えなければ、理由を報告して手動の確認に回す）
- internal/tui の土台: term・keys・screen を組み合わせたイベントループ、panic とシグナルからの復元
- cmd/tuiprobe に確認用の画面を足す: 2 ペイン・10 万行の一覧、中央の入力欄（lineedit と本物のカーソル）、
  進捗の更新、panic とシグナルの試験、大きさの変更
- 手動の確認（VU1・VU4・VU5・VU9・VT4・VT5・VT7）: 画面の撮影とマウスで行える操作（ウィンドウの大きさの変更など）はあなたが行い、
  キー入力と IME は、仮想マシン・Mac の端末とも、手順を示して私に頼む（CLAUDE.md の「仮想マシンでの確認」）

完了条件:
- term が tui §10 の完了条件を満たす
- filer §12 の VU1 の受け入れの条件を、対象のすべての端末で満たす。満たさないものは、原因と対処の案を報告する
- VU4・VU5・VU9・VT4・VT5・VT7 の結果の表と、仕様の変更案を示す
```

### フェーズ17: fsops への追加（F1・F2）

```
フェーズ17です。docs/SPEC-filer.md の §6、§11 と、docs/SPEC-fsops.md の §1、§4、§5、§8.2、§11.3、§14.1、§17 を読んでください。

まだコードは書かないでください。次のことを示してください。
- F1 の案 a と案 b の比較（filer §11 の懸念を含む）と、どちらをすすめるか、その理由
- F2（Mkdir）の API と、SPEC-fsops の変更後の文面
私が承認したら SPEC-fsops を更新し、テストを先に書いてから実装してください。

完了条件:
- CI が 3 つの OS で成功する
- 不変条件の経路とテストの対応表を報告する
```

### フェーズ18: 読むだけのファイラー

```
フェーズ18です。docs/SPEC-filer.md の §2〜§7、§9、§10、§12（VU7・VU8・VU10）、§15 を読んでください。

やること:
- internal/listing、internal/textfmt、internal/app（閲覧の部分）、internal/tui（メイン画面）、cmd/tana
- app はペインの並びとして作り、画面の構成に依存させない（filer §4）。まず 2 ペインで作る
- 関連付けで開く操作（実行ファイルの確認と、VU10 を確かめるまでの制限を含む）
- VU7・VU8・VU10 を確かめる
- ゴールデンファイル（80×24、120×40）

完了条件:
- CI が 3 つの OS で成功する
- 私が Mac と仮想マシンで触るための手順を示す。触った結果をもとに、画面の構成とキーの系統（filer §15）を決める。
  迷ったら、ミラーカラムの表示の試作を相談する
```

### フェーズ19: コピー・移動

```
フェーズ19です。docs/SPEC-filer.md の §2、§8.1〜§8.5、§8.8、§10 と、internal/fsops/doc.go を読んでください。

やること:
- internal/msg（すべての Kind・Outcome・段階の文言と、漏れがないことのテスト）。cmd/fsopsctl をこれに切り替える
- コピー・移動の流れ（filer §8.1〜§8.5）: 確認、衝突の決定、進捗とキャンセル、結果
- U1〜U3 のテストを先に書く（未選択の衝突がスキップになる、先行入力で確定しない、完了以外の結果を隠さない、など）
- ゴールデンファイル

完了条件:
- CI が 3 つの OS で成功する
- U1〜U6 を破りうるコード経路と、それを防ぐテストの対応表を報告する
- 私が手で確かめる手順を示す
```

### フェーズ20: ごみ箱・完全削除・名前の変更・新しいフォルダ

```
フェーズ20です。docs/SPEC-filer.md の §2、§8.2、§8.5〜§8.8 を読んでください。

やること:
- ごみ箱（ごみ箱に入らなかった項目から完全削除の確認へ進む流れ。OutcomeTrashUnconfirmed の扱いを含む）
- 完全削除（y だけで確定する。先行入力で確定しない）
- 名前の変更（lineedit、fsops.Rename のエラーの表示）と新しいフォルダ（F2）
- U2・U4・U5 のテストを先に書く（fsops の I5 の UI 側を含む）
- VU5（ごみ箱の確認ダイアログが前面に出るか。filer §12.4。フェーズ16から回した）: 試験用のボリュームのごみ箱の最大サイズを小さくする設定を、手順を示して私に頼む

完了条件:
- CI が 3 つの OS で成功する
- U1〜U6 の対応表を報告する
- 私が手で確かめる手順を示す
- VU5 の結果を報告する
```

### フェーズ21: 手動の確認と仕様への反映

```
フェーズ21です。docs/SPEC-filer.md の §12、§13、§15 と、CLAUDE.md の「仮想マシンでの確認」を読んでください。

やること:
- filer §12 と tui §9 のうち、まだ結果のない項目を確かめる
- 仮想マシンの Windows Terminal と conhost で、v0.1 のすべての操作を一通り行う（キーで行う操作は、手順書を作って私に頼む。画面の確認はあなたが行う）
- Mac の端末で私が行う確認の手順書を作る
- 結果を filer §12 に追記し、仕様と実装のずれを一覧にする

完了条件:
- 確認の記録（何を、どの端末とその版で確かめたか）がそろっている
- ずれの一覧と、直す順番の案を報告する
```

---

## 3. 困ったとき用のプロンプト

### CI の Windows だけ失敗する

```
GitHub Actions の Windows ジョブが失敗しました。gh run view --log-failed でログを確認し、原因を特定して報告してください。
修正の前に、なぜ macOS では通ったのかを説明してください。
Windows 固有の挙動が原因なら、それを再現する最小のテストを先に追加してから直してください。
```

### テストがときどき失敗する

```
<テスト名> が CI でときどき失敗します。gh で直近の失敗ログを集め、原因の候補を挙げてください
（タイミング依存、ウイルス対策ソフトによる一時的なロック、一時フォルダの扱いなど）。
テストにスリープやリトライを足して隠す修正はしないでください。
fsops の実装側で共有違反に短いリトライを入れるべきだと考える場合は、その案と不変条件への影響を示して私に確認してください。
```

### 端末で表示が崩れる・キーが効かない

```
<端末の名前と版> で <症状> が起きます。
cmd/tuiprobe の width・keys でその端末を測り直し、docs/probe-results の以前の結果と比べて、原因を特定して報告してください。
直す前に、測った結果（またはキーの記録）を入力にして症状を再現するテストを、textwidth・keys・screen のうち原因のあるパッケージに追加してください。
```

### 仕様を変えたい

```
<変えたい内容> のように仕様を変えたいです。
SPEC のどの節に影響するか、§2 の不変条件と衝突しないかを確認し、SPEC の変更案とテストへの影響を示してください。
私が承認するまで実装しないでください。
```

### 不変条件の監査（いつでも）

```
現在の internal/fsops のコードを、docs/SPEC-fsops.md §2 の不変条件 I1〜I7 の観点だけで見直してください。
各不変条件について、それを破りうるコード経路と、それを防いでいるテストを対応づけて示し、
テストで守られていない経路があれば列挙してください。修正はまだしないでください。
```

---

## 4. CI 以外での確認（リリース前）

CI では確かめられないものがあります（SPEC §20 の V9 など）。

- 日本語ロケールの Windows での動作
- OneDrive の同期フォルダ（未ダウンロードのファイル）
- ネットワークドライブ、USB メモリ（Windows の FAT32・exFAT、macOS の exFAT）

UTM などの仮想マシンの Windows、または実際に使う Windows PC で、フェーズ11の `fsopsctl` を使って確認してください。
Windows 用のバイナリは Mac 上で作れます。

```sh
GOOS=windows GOARCH=amd64 go build -o fsopsctl.exe ./cmd/fsopsctl   # 一般的な PC 用
GOOS=windows GOARCH=arm64 go build -o fsopsctl.exe ./cmd/fsopsctl   # Apple Silicon の Mac 上の Windows 用
```

macOS 用のバイナリは、ごみ箱に cgo を使うため、Mac 上で `CGO_ENABLED=1`（既定）でビルドしてください。
ほかの OS からクロスコンパイルすると、ごみ箱が常に使えないビルドになります。

---

## 5. 仮想マシンでの確認（Parallels Desktop）

端末の見え方やキー入力は、CI では確かめられません（SPEC-filer §12）。
そこで、Parallels Desktop の仮想マシン「Windows 11」（ARM 版）で確かめます。
Claude Code は、`prlctl exec` で端末とプローブを起動し、computer use で画面を撮影します。
キー入力と IME は、あなたが実物のキーボードで行います。フェーズ12で、computer use で送るキーは当てにならないと分かったためです（次の「注意」）。

### 準備（1 回だけ。あなたが行う）

1. 仮想マシンで、日本語の IME が使えることを確かめる（なければ Windows の設定で追加する）。
2. Claude Code の desktop app で computer use を使えるようにする。初めて使うときに、Parallels Desktop へのアクセスの許可を求められるので、許可する。
3. `prlctl exec`（仮想マシンの中でコマンドを実行する）を使うには、仮想マシンに Parallels Tools が入っている必要がある。

### 毎回の流れ（Claude Code が行う）

1. `prlctl start "Windows 11"` で仮想マシンを起動する。
2. `GOOS=windows GOARCH=arm64` でビルドし、リポジトリの `.vmstage`（git に入れない）と共有フォルダ（`Z:`）を通して、
   仮想マシンのローカルのプローブ用のフォルダ（`%USERPROFILE%\tana-probe`）に置く。
3. 入力の要らないもの（width など）は、`prlctl exec` で Windows Terminal（`wt.exe`）と conhost（`conhost.exe`）からプローブを起動し、computer use で画面を撮影する。
4. キー入力が要るものは、ダブルクリックで起動できるファイル（`.cmd`）をプローブ用のフォルダに用意し、手順を示してあなたに頼む。
5. 結果のファイルを `docs/probe-results` に取り込む。

### あなたにお願いすること

- 仮想マシンと Mac の端末（Terminal.app・iTerm2）での、キー入力と IME の確認。
  プローブの案内に従ってキーを押すだけで、記録はプローブが行う（1 端末あたり 5〜15 分の見込み）。押し間違えた手順は、`-steps` で撮り直せる。
- 仮想マシンの設定の変更が必要になった場合の変更（Claude Code はシステムの設定を変えない）。

### 注意

- 仮想マシンは ARM 版の Windows 11 です。x64 の PC や Windows 10 の conhost とは違うことがあるので、結果には OS と端末の版を記録します。
- 共有フォルダ（`\\Mac\...`）はネットワークドライブとして扱われ、ごみ箱が使えず、SmartScreen に止められることもあります。プローブは仮想マシンのローカルのフォルダで動かします。
- computer use で送るキーは、仮想マシンでは当てになりません（フェーズ12）。
  - Esc は computer use の停止のキーなので届きません。Claude Code が操作している間にあなたが Esc を押すと、Claude Code の操作が止まります。
  - 速い文字の入力は、文字が落ちます。文字のキーは Windows 11 の長押しの候補（Å など）を開き、後のキーを飲み込みます。
  - Mac の Option が Ctrl として届くことがあります。
- conhost では、プローブの入力モードで Ctrl＋V が貼り付けになりません。貼り付けは右クリックのメニューで行います。
